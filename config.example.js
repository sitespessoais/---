/**
 * Copie para config.js → URL + anon key (Supabase → Project Settings → API).
 * Tabelas novas: supabase-tabelas-iniciais.sql | upload 403: supabase-storage.sql
 * Não faça commit do config.js (está no .gitignore).
 */
window.ALBUM_SYNC = {
  supabaseUrl: "https://O-TEU-PROJECTO.supabase.co",
  supabaseAnonKey: "COLA-AQUI-A-ANON-KEY-DO-DASHBOARD",
  bucket: "magens",
  table: "album_entries",
};
